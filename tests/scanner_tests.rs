// Scanner module tests — placeholders for future scanner functionality.

#[test]
fn scanner_module_is_accessible() {
    // Verify the scanner module is publicly reachable from the crate.
    // Actual scanner tests will be added when the module is implemented.
    let _ = netascan::scanner::module_path();
}
