# Skill Registry — net-audit-scanner

> Generated by **sdd-init** (re-init) — 2026-05-13
> Mode: `hybrid` | Persistence: Engram + OpenSpec

## Project-level Skills

None found (no `.claude/skills/`, `skills/`, or `.agent/` in project root).

## User-level Skills (resolved)

All skills sourced from `/home/jalonso/.config/opencode/skills/` and `/home/jalonso/.claude/skills/`.

### Testing & Verification

| Skill | Path | Compact Rules |
|-------|------|---------------|
| **pytest** | `.../skills/pytest/SKILL.md` | Use `pytest.raises` for expected exceptions; `@pytest.fixture` for shared state; scopes: `function` (default), `class`, `module`, `session`. Prefer `conftest.py` for shared fixtures across test files. Use `tmp_path` built-in fixture for temp files, not custom cleanup. Parametrize with `@pytest.mark.parametrize`. Markers: `slow`, `integration`, `e2e`. Mock external calls with `monkeypatch` or `unittest.mock`. |
| **go-testing** | `.../skills/go-testing/SKILL.md` | Not applicable — project stack is Rust + Python. |
| **playwright** | `.../skills/playwright/SKILL.md` | Not applicable — no browser-based E2E in this project. |

### Commit & PR Workflow

| Skill | Path | Compact Rules |
|-------|------|---------------|
| **work-unit-commits** | `.../skills/work-unit-commits/SKILL.md` | Each commit = one deliverable work unit (behavior, fix, migration, docs). Keep tests + docs with the code they verify. Never commit by file type (models, then services, then tests). Keep each PR under 400 changed lines. Commit messages explain outcome, not file list. |
| **branch-pr** | `.../skills/branch-pr/SKILL.md` | Every PR MUST link an approved issue. Branch names: `type/description` lowercase (`feat/`, `fix/`, `chore/`, `docs/`, `refactor/`, `perf/`, `test/`, `ci/`). Exactly one `type:*` label per PR. Run automated checks before merge. |
| **chained-pr** | `.../skills/chained-pr/SKILL.md` | Split PRs over 400 changed lines. Each PR reviewable in ≤60 min. Use Stacked PRs (to main) when slices land independently. Use Feature Branch Chain with tracker PR when feature must integrate before main. Include dependency diagram in child PRs. |
| **issue-creation** | `.../skills/issue-creation/SKILL.md` | Must use template (bug report or feature request). Every issue auto-labeled `status:needs-review`. Maintainer adds `status:approved` before any PR. Questions go to Discussions, not issues. Pre-flight: check duplicates, understand approval workflow. |

### Review & Quality

| Skill | Path | Compact Rules |
|-------|------|---------------|
| **judgment-day** | `.../skills/judgment-day/SKILL.md` | Launch two blind judges in parallel — never review yourself. Classify warnings as `WARNING (real)` only if normal intended use triggers them. After fixes, re-judge before commit/push/done. Terminal states: `APPROVED` or `ESCALATED`. Max 2 fix iterations before asking user. |
| **pr-review** | `.../skills/pr-review/SKILL.md` | Structured analysis for open source projects. Review PRs, analyze issues, audit backlog. Check what's open, pending, unattended. |
| **comment-writer** | `.../skills/comment-writer/SKILL.md` | Lead with actionable point. Be warm and direct, not corporate. Keep 1-3 paragraphs or short bullet list. Explain WHY for each requested change. Match thread language (Spanish → Rioplatense voseo). No em dashes. Avoid pile-ons. |
| **technical-review** | `.../skills/technical-review/SKILL.md` | Structured evaluation for technical exercises and candidate submissions. Use when reviewing code assessments or take-home tests. |

### Documentation & Design

| Skill | Path | Compact Rules |
|-------|------|---------------|
| **cognitive-doc-design** | `.../skills/cognitive-doc-design/SKILL.md` | Lead with the answer/outcome. Progressive disclosure: happy path first, then edge cases. Chunk related info into small sections. Signpost with headings and callouts. Prefer tables, checklists, examples over prose. Design for reviewer empathy. |
| **stream-deck** | `.../skills/stream-deck/SKILL.md` | Slide-deck presentations for streams/courses. Uses Gentleman Kanagawa Blur theme. Inline SVG diagrams. Not applicable for current project (no presentation need yet). |

### Infrastructure & Platforms

| Skill | Path | Compact Rules |
|-------|------|---------------|
| **skill-creator** | `.../skills/skill-creator/SKILL.md` | LLM-first skills with valid frontmatter. Required structure: frontmatter, Activation Contract, Hard Rules, Decision Gates, Execution Steps, Output Contract, References. Description quoted, one line, trigger-first, ≤250 chars. 180-450 body tokens. |
| **homebrew-release** | `.../skills/homebrew-release/SKILL.md` | Release workflow for `gentleman-programming/homebrew-tap`. Not applicable — this project produces a Linux binary, not macOS homebrew formula. |

### Web/Frontend (not applicable)

| Skill | Reason for exclusion |
|-------|---------------------|
| typescript, zod-4, zustand-5, react-19, tailwind-4 | Web frontend stack — not applicable to Rust CLI + web server project. |
| nextjs-15 | Next.js App Router — not applicable. |
| ai-sdk-5 | Vercel AI SDK — not applicable. |
| angular | Angular 20+ — not applicable. |
| dotnet, django-drf | .NET, Django — not applicable. |
| chrome-extension-mv3, cdp-chrome-debugger | Chrome extensions — not applicable. |

## Convention Files

None found in project root (no `AGENTS.md`, `CLAUDE.md`, `.cursorrules`, `GEMINI.md`, or `copilot-instructions.md`).

## Notes

- SDD skills (`sdd-*`), `_shared`, and `skill-registry` are excluded from this registry per scan rules.
- Registry is reference-only; skills are loaded on-demand via the `skill()` tool when the task matches their trigger.
- Rebuild this registry when new skills are added or project structure changes.
